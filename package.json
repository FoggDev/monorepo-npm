{
  "name": "monorepo-npm",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "npm run build -w @monorepo/ui && npm run build -w @monorepo/utils && npm run build -w @monorepo/frontend",
    "dev": "npm run dev -w @monorepo/frontend"
  }
}
